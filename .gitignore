# macOS
.DS_Store

# Xcode
*xcuserdata*
*xccheckout*
*.xcscmblueprint
IDEWorkspaceChecks.plist
DerivedData/
*.moved-aside
*.hmap

# Swift Package Manager
.swiftpm/
.build/
# Root Package.resolved is not committed for libraries — consumers manage their own resolution.
# The workspace Package.resolved (ButterflyMXSDK.xcworkspace/xcshareddata/swiftpm/Package.resolved)
# should be committed for reproducible team development builds.
/Package.resolved

# CocoaPods (not supported)
Pods/

# Legacy
BMXCall/Dependencies/pjsip
